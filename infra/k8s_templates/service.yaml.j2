apiVersion: v1
kind: Service
metadata:
  name: {{ name }}
  namespace: uni-space
  labels: {app: {{ name }}}
spec:
  selector: {app: {{ name }}}
  ports:
    - name: {{ name }}
      protocol: TCP
      port: 80               
      targetPort: {{ port }} 