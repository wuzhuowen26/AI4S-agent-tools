apiVersion: v1
kind: Service
metadata:
  name: {{ name }}
  namespace: deepmodeling
  labels: {app: {{ name }}}
spec:
  selector: {app: {{ name }}}
  ports:
    - name: {{ name }}
      protocol: TCP
      port: 80               
      targetPort: {{ port }} 