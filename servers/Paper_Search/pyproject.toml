[project]
name = "paper-search"
version = "0.1.0"
description = "Paper Search Service"
requires-python = ">=3.12"

dependencies = [
    "arxiv==1.4.6",
    "fastapi",
    "uvicorn",
    "fastmcp"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build" 